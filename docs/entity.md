Entities are the core data objects in Entu. Perform full CRUD operations - create, read, update, and delete entities.

List entities with advanced filtering (exact match, regex, multiple values), sorting, grouping, and pagination. Search across entities, view entity change history, duplicate entities, and get aggregated data with computed fields.

Entities can have properties of various types including strings, numbers, dates, references, files, and more.
